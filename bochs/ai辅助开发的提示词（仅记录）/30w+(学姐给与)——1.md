你现在接手的是一个 **对标 Windows 版 Bochs 的最小热路径提取工程**。
当前目标不是重写 Bochs，也不是重新设计一个模拟器，更不是自己手写一套新的 x86 模拟执行框架。当前工作的本质是：**以 Windows 版 Bochs 源码为标准答案，从 Bochs 源码中复制、裁剪、粘贴出一条最小必要执行链路**，使我们自己的工程能够沿着 Bochs 的行为继续推进，最终看到 Debian 13 的登录页面。
可以没有图形化界面，但必须能走到最后的串口/终端登录输出。
**一、当前工程背景**
1. 我们自己的代码基本是从 Bochs 源码中裁剪、复制、迁移出来的。
2. 目前 Bochs 源码侧和我们自己的工程侧都采用单步执行方式。
3. 我们已经植入了 MD5 计算逻辑，用于和 Bochs 源码侧逐步对齐。
4. MD5 当前计算的是 20 个通用寄存器的值，用于判断两边 CPU 状态是否一致。
5. 我个人的工程中不保留 debug、log、debugger 相关内容，也关闭了大量无关宏，例如：

这些功能在我们的最小链路中都不需要。
**二、当前进展**
1. 我们自己的工程已经可以和 Bochs 源码对齐到大约 30 万条指令。
2. 30 万条之后，开始进入设备相关路径，需要逐步补充必要设备逻辑。
3. 后续可能会因为时钟、计数器、异步状态等原因导致 MD5 不一致。
4. 判断是否为时钟相关影响的方法很简单：重新运行一遍 Bochs 源码侧，如果源码侧两次运行的 MD5 本身就不一致，就说明差异大概率来自时钟或动态状态，而不是我们迁移代码的确定性错误。
5. 遇到这种情况时，可以调整 MD5 的寄存器覆盖范围，不再拷贝全部 20 个寄存器，而是从第 4 个寄存器开始拷贝到第 20 个寄存器，用于规避前几个寄存器中可能包含的动态值干扰。
**三、最重要的工作方式**
当前工程的核心要求是：
**99% 的工作都应该是复制 Bochs 源码、裁剪 Bochs 源码、粘贴 Bochs 源码。**
bochs源码的位置在"D:\bochs-3.0"，你可以直接运行但是不能做修改，也不能添加任何东西。
不要优先自己写代码。不要重新设计逻辑。不要用自己的理解重写 Bochs 行为。不要为了“简化”而改变 Bochs 的关键副作用。不要为了“代码更干净”而改写 Bochs 的执行语义。
如果某个行为 Bochs 已经实现了，优先直接从 Bochs 源码里复制对应实现，再做最小适配。
当前项目不是做原创实现，而是做 **Bochs 最小热路径提取**。判断代码是否正确的标准不是它是否优雅，而是它是否能让我们自己的工程继续和 Bochs 源码侧保持 MD5 对齐。
**四、允许你做的事情**
1. 你可以直接打开 Bochs 源码，根据需要大胆复制源码中的实现。
2. 你可以从 Bochs 的 CPU、内存、设备、中断、IO、时钟等路径中提取必要代码。
    
3. 你可以复制 Bochs 的函数、结构体、宏、枚举、状态更新逻辑。
    
4. 你可以删除不需要的分支、配置项、debug 路径、log 路径、trace 路径。
    
5. 你可以把 Bochs 中依赖动态配置的值替换成我们当前场景下的静态值。
    
6. 你可以临时加入少量打印或调试代码用于定位问题，但最终应尽量清理掉。
    

**五、不希望你做的事情**

1. 不要自己重新实现 Bochs 已经有的逻辑。
    
2. 不要自行设计新的设备模型。
    
3. 不要自行设计新的 CPU 执行框架。
    
4. 不要自行设计新的中断、IO、时钟、内存访问抽象。
    
5. 不要引入复杂 C++ 容器。
    
6. 不要引入复杂类层级。
    
7. 不要引入额外框架。
    
8. 不要把 Bochs 的完整系统全部搬过来。
    
9. 不要保留和 Debian 13 最小启动链路无关的功能。
    
10. 不要追求完整模拟器，只追求当前热路径能继续推进。
    

**六、关于动态值和静态值**

我们不需要 Bochs 的完整动态配置系统。

对于 sim 相关的值、动态识别的值、运行时配置值，如果在当前 Debian 13 最小启动链路中可以确定，就必须直接写成静态值。

也就是说：

1. 不需要保留完整配置解析。
    
2. 不需要保留完整参数系统。
    
3. 不需要保留完整设备注册系统。
    
4. 不需要保留完整运行时动态识别逻辑。
    
5. 能静态化的值就静态化。
    
6. 只保留对当前热路径有实际影响的值和逻辑。
    

但需要注意：静态化不能破坏 Bochs 原本的关键行为。如果某个动态值会影响 CPU 状态、设备状态、中断状态、IO 返回值或 MD5 对齐，就必须谨慎处理。

**七、当前推进目标**

1. 继续沿着 Bochs 源码侧和我们自己的工程侧进行 MD5 对齐。
    
2. 当前优先目标是从 30 万条指令继续向后推进。
    
3. 后续目标是持续推进到 5000 万条指令级别的 MD5 对齐。
    
4. 为了节省比对时间，可以采用分段打印策略，例如每 100 万条指令打印 10 条关键 MD5 状态，用于快速定位分叉区间。
    
5. 每次分叉后，不要盲目重写逻辑，应优先回到 Bochs 源码中寻找对应路径，然后复制最小必要实现。
    

**八、发现 MD5 分叉时的判断顺序**

一旦发现 MD5 分叉，应优先判断：

1. 是否是时钟、计数器或动态状态导致；
    
2. 是否是寄存器覆盖范围导致；
    
3. 是否是设备路径缺失；
    
4. 是否是 IO 读写返回值不一致；
    
5. 是否是中断状态不一致；
    
6. 是否是内存映射或 MMIO 行为不一致；
    
7. 是否是 Bochs 某段必要逻辑没有迁移；
    
8. 是否是我们把某个动态值错误地静态化；
    
9. 是否是裁剪时误删了关键副作用；
    
10. 是否是我们自己手写替代逻辑时改变了 Bochs 行为。
    

判断是否为时钟相关影响的方法：

重新运行一遍 Bochs 源码侧。如果 Bochs 源码侧两次运行的 MD5 本身就不一致，则说明差异大概率来自时钟、计数器或其他动态状态。

此时可以考虑调整 MD5 覆盖范围，例如不再拷贝全部 20 个通用寄存器，而是从第 4 个寄存器开始拷贝到第 20 个寄存器，用于避开前几个可能受动态状态影响的寄存器。

**九、每次修改时必须说明的内容**

每次补充代码时，优先说明以下内容：

1. 分叉发生在哪个 step；
    
2. 分叉时的 PC 是多少；
    
3. Bochs 源码侧的 MD5 是什么；
    
4. 我们工程侧的 MD5 是什么；
    
5. Bochs 源码侧走到了哪个函数；
    
6. 我们工程侧缺少哪段逻辑；
    
7. 需要从 Bochs 哪些文件复制哪些最小代码；
    
8. 哪些代码必须保留；
    
9. 哪些代码可以裁剪；
    
10. 哪些动态值可以改成静态值；
    
11. 修改后如何重新验证；
    
12. 当前修改是否继续保持最小热路径原则。
    

**十、代码处理原则**

1. 优先复制 Bochs 源码中的原始实现。
    
2. 只在必要时做最小适配。
    
3. 不要主动重构成另一套架构。
    
4. 不要为了“看起来更干净”而改变 Bochs 原有行为。
    
5. 不要引入 C++ 容器、复杂抽象、复杂类层级或额外框架。
    
6. 不要引入和最小启动链路无关的功能。
    
7. 不需要实现完整 Bochs，只需要实现能支撑当前 Debian 13 最小热路径继续向后跑的必要子集。
    
8. 所有修改都必须服务于 MD5 对齐和最终登录页面输出。
    
9. 如果某段代码能直接从 Bochs 复制，就不要自己写替代版本。
    
10. 如果必须自己写适配代码，也只能写胶水层、静态值替换层、接口适配层，不要重写核心行为。
    

**十一、最终判断标准**

当前最重要的判断标准不是代码是否完整，也不是代码是否优雅，而是：

1. 能不能继续和 Bochs 源码侧保持 MD5 对齐；
    
2. 能不能让指令数继续向后推进；
    
3. 能不能逐步补齐必要设备路径；
    
4. 能不能最终看到 Debian 13 登录页面；
    
5. 能不能在不搬完整 Bochs 的前提下，提取出最小必要热路径。
    

请按照这个原则继续推进：

**以 Bochs 源码为标准答案，99% 复制 Bochs 源码，最小裁剪、最小迁移、最小适配，持续推进 MD5 对齐，直到 Debian 13 登录页面。**

你现在继续接手当前文件夹下的 **对标 Bochs 最小热路径提取工程**。

当前工程的基本情况如下：

1. 我们在当前工程代码和 Bochs 源码中都植入了 MD5 计算逻辑。
    

2. 两边目前都是单步执行。
    

3. 每执行一条指令后，通过比对 MD5 和 RIP 判断当前指令执行结果是否一致。
    

4. MD5 用于检测寄存器状态是否一致，RIP 用于判断控制流是否一致。
    

5. 当前工作的核心仍然不是重写模拟器，而是从 Bochs 源码中复制、裁剪、粘贴最小必要代码到我们自己的工程中。
    

你接下来的工作是：

1. 继续在当前文件夹下推进热路径。
    
2. 补充当前缺失的指令、设备、IO、中断等必要逻辑。
    
3. 找到我们工程和 Bochs 源码之间的第一个稳定分叉点。
    
4. 分析分叉点产生的原因。
    
5. 在我们自己的工程代码中进行 debug。
    
6. 从 Bochs 源码中复制对应的最小必要实现到我们自己的工程中，bochs源码的位置在"D:\bochs-3.0"。
    
7. 尽量不要自己重新写逻辑，99% 的工作应该是复制 Bochs 源码并做最小适配。
    

当前阶段的重点工作是：**补充设备路径**。

需要特别注意时钟中断和动态事件带来的影响。由于时钟中断、计数器、异步设备状态等因素，Bochs 源码侧和我们自己的工程侧可能会从某一条指令开始出现 MD5 和 RIP 都对不上的情况。这种情况不一定是指令实现错误，也可能是中断时机不同导致执行流发生偏移。

判断分叉原因时，按下面的顺序处理：

1. 先看 RIP 是否一致。
    
2. 如果 RIP 不一致，优先怀疑是中断、时钟、设备事件或异步状态导致控制流不同。
    
3. 遇到 RIP 不一致时，可以多次运行 Bochs 源码侧，观察 Bochs 每次运行结果是否稳定。
    
4. 如果 Bochs 源码侧多次运行的 MD5/RIP 本身就不一致，说明差异大概率来自时钟中断或动态事件。
    
5. 这种情况下，可以多次运行 Bochs 源码侧，找到和我们工程输出相同或最接近的一条路径，再继续对齐。
    
6. 如果 RIP 一致，但 MD5 不一致，则优先怀疑是某条指令、寄存器更新、flag 更新、内存访问、IO 副作用或设备状态更新不一致。
    
7. 如果 RIP 一致但 MD5 不一致，应重点回溯最近几条指令，定位是哪一次状态更新出现偏差。
    

对 Bochs 源码侧的要求：

1. 尽量不要修改 Bochs 源码。
    
2. 除非只是为了打印寄存器、设备状态、IO 值、中断状态等调试信息，否则不要改变 Bochs 源码逻辑。
    
3. Bochs 源码侧应作为标准答案，不要在源码侧修逻辑。
    
4. 真正的修改应尽量发生在我们自己的工程代码中。
    
5. 如果需要调试 Bochs，可以临时增加打印，但不要改变 Bochs 的执行行为。
    

当前已知对齐点如下：

我们自己的工程输出：

1100009: f1fd716c RIP=918426 [BIOS info] PIIX3/PIIX4 init: elcr=00 0aBochs 源码输出：1100009: f1fd716c RIP=918426

这说明至少到 step=1100009 时，两边的 MD5 和 RIP 仍然一致。后面应该还有不少指令可以继续对上。

当前打印策略比较粗，是每隔5万条打印几条，因此不利于精确定位分叉点。你可以根据需要调整打印策略，例如：

1. 先扩大区间，快速确认大致分叉范围；
    
2. 再缩小到几千条、几百条、几十条；
    
3. 最后定位到第一条 MD5/RIP 不一致的指令；
    
4. 对关键区间可以连续打印每一条指令的 step / MD5 / RIP / opcode / 关键寄存器；
    
5. 定位完成后，再恢复较低频率打印，避免输出过大。
    

这一轮的目标是：

**继续补充设备相关最小必要代码，把我们自己的工程和 Bochs 源码侧的 MD5 对齐推进到 8000 万条指令。**

工作原则保持不变：

1. 以 Bochs 源码为标准答案。
    
2. 优先复制 Bochs 原始实现。
    
3. 只做最小裁剪和最小适配。
    
4. 不主动重写 Bochs 已有逻辑。
    
5. 不引入复杂新架构。
    
6. 不搬完整 Bochs，只搬当前热路径需要的最小代码。
    
7. 所有修改都服务于继续推进 MD5/RIP 对齐。
    
8. 如果某个逻辑 Bochs 已经有实现，就不要自己写替代版本。
    

每次修改后，请说明：

1. 当前分叉发生在哪个 step；
    
2. 分叉时两边的 RIP 是否一致；
    
3. 分叉时两边的 MD5 分别是什么；
    
4. Bochs 源码侧走到了哪个函数或设备路径；
    
5. 我们自己的工程缺少了哪段逻辑；
    
6. 从 Bochs 哪些文件复制了哪些最小代码；
    
7. 哪些代码被裁剪；
    
8. 哪些动态值被静态化；
    
9. 修改后重新验证到了多少条指令；
    
10. 是否继续满足最小热路径提取原则。
    

最终目标不是代码完整，也不是代码优雅，而是：

**在尽量少搬代码的前提下，让我们自己的工程持续对齐 Bochs，尽快推进到 8000 万条 MD5 对齐。**