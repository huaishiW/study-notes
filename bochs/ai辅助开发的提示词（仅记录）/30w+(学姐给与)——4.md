你现在继续接手当前文件夹下的对标 Bochs 最小热路径提取工程。

当前工程已经推进到了较远的指令数位置，本轮暂时不需要继续运行 Bochs 源码侧，也不需要继续做 Bochs 源码侧和我们工程侧的 MD5/RIP 对齐比对。

本轮任务目标非常明确：

只需要在我们自己的工程代码中补充所有 FPU / 浮点运算相关指令，使当前工程具备更完整的浮点指令执行能力，并确保代码能够成功编译、运行。

本轮工作重点：

1. 在当前工程中查找所有尚未实现、实现不完整、返回 stub、直接报错、或者行为明显不完整的 FPU / 浮点相关指令。
    
2. 对照 Bochs 源码中的对应实现，bochs源码的位置在"D:\bochs-3.0"。
    
3. 将 Bochs 源码中的 FPU / 浮点指令逻辑复制、裁剪、迁移到我们自己的工程中。
    
4. 补齐必要的 FPU 状态结构、寄存器访问逻辑、异常标志、控制字、状态字、tag word、rounding、precision、NaN/Inf/denormal 等相关处理。
    
5. 修改后确保当前工程能够成功编译。
    
6. 编译通过后，进行基本运行验证，确认不会因为 FPU 指令缺失导致直接崩溃或退出。
    
7. 当 FPU / 浮点相关指令补充完成，并且工程可以成功编译运行后，本轮任务结束。
    

本轮仍然保持之前的代码原则：

1. 99% 的工作应当是从 Bochs 源码中复制、裁剪、粘贴最小必要代码。
    
2. 不要自己重新设计 FPU 执行逻辑。
    
3. 不要自己手写一套新的浮点模拟器。
    
4. 如果 Bochs 源码中已经有对应实现，就优先复制 Bochs 原始实现。
    
5. 只做最小适配，不做主动重构。
    
6. 不要引入复杂 C++ 容器、复杂类层级或额外框架。
    
7. 不要为了代码看起来更干净而改变 Bochs 的浮点语义。
    
8. 不要搬完整 Bochs，只搬当前工程补齐 FPU 所需的最小代码。
    
9. 如果必须自己写代码，只允许写胶水层、接口适配层、静态值替换层，不要重写核心浮点行为。
    

本轮不需要做的事情：

1. 不需要继续运行 Bochs 源码侧。
    
2. 不需要继续寻找新的 MD5/RIP 分叉点。
    
3. 不需要继续分析时钟中断影响。
    
4. 不需要继续多次运行 Bochs 源码判断动态事件。
    
5. 不需要大规模补设备。
    
6. 不需要推进到新的指令数目标。
    
7. 不需要优化性能。
    
8. 不需要做完整 Bochs 级别的 FPU 子系统，只需要补齐我们当前工程中缺失的 FPU / 浮点指令路径。
    

建议处理顺序：

1. 先全局搜索当前工程中 FPU / x87 / MMX / SSE / floating / float / softfloat / fpu 指令相关的 stub、TODO、未实现函数、空函数、直接报错函数。
    
2. 对照 Bochs 源码找到对应文件和对应函数。
    
3. 优先复制最核心的 x87 FPU 指令执行逻辑。
    
4. 再补充 FPU 寄存器栈相关逻辑，例如 ST0-ST7、TOP、tag word、push/pop、empty 标记。
    
5. 再补充 FPU 控制字和状态字相关逻辑。
    
6. 再补充浮点异常、舍入、比较、转换、加载、存储等辅助逻辑。
    
7. 如果当前工程已经进入 SSE/MMX 相关路径，再继续补充对应的 SSE/MMX 浮点相关指令。
    
8. 每补一批代码，就编译一次，避免一次性搬运过多导致错误难以定位。
    

重点关注的 FPU 指令类型包括但不限于：

1. x87 数据加载和存储：
    
    - FLD
        
    - FST
        
    - FSTP
        
    - FILD
        
    - FIST
        
    - FISTP
        
    - FBSTP
        
    - FBLD
        
2. x87 基本运算：
    
    - FADD
        
    - FADDP
        
    - FSUB
        
    - FSUBP
        
    - FSUBR
        
    - FSUBRP
        
    - FMUL
        
    - FMULP
        
    - FDIV
        
    - FDIVP
        
    - FDIVR
        
    - FDIVRP
        
3. x87 比较指令：
    
    - FCOM
        
    - FCOMP
        
    - FCOMPP
        
    - FUCOM
        
    - FUCOMP
        
    - FUCOMPP
        
    - FCOMI
        
    - FCOMIP
        
    - FUCOMI
        
    - FUCOMIP
        
4. x87 控制和状态指令：
    
    - FINIT / FNINIT
        
    - FLDCW
        
    - FSTCW / FNSTCW
        
    - FSTSW / FNSTSW
        
    - FCLEX / FNCLEX
        
    - FWAIT
        
    - FNOP
        
5. x87 栈和寄存器操作：
    
    - FXCH
        
    - FFREE
        
    - FINCSTP
        
    - FDECSTP
        
6. x87 常量和特殊运算：
    
    - FLD1
        
    - FLDZ
        
    - FLDPI
        
    - FLDL2E
        
    - FLDL2T
        
    - FLDLG2
        
    - FLDLN2
        
    - FABS
        
    - FCHS
        
    - FSQRT
        
    - FRNDINT
        
    - FSCALE
        
    - FXTRACT
        
    - FPATAN
        
    - FYL2X
        
    - FSIN
        
    - FCOS
        
    - FSINCOS
        
    - FPTAN
        
7. FPU 环境保存和恢复：
    
    - FSAVE / FNSAVE
        
    - FRSTOR
        
    - FSTENV / FNSTENV
        
    - FLDENV
        
    - FXSAVE
        
    - FXRSTOR
        

如果 Bochs 源码将某些 FPU 功能拆分在多个文件中，请按依赖关系复制最小必要部分。不要只复制指令入口函数，还要检查它依赖的辅助函数、宏、状态字段、异常处理函数和软浮点函数是否也需要迁移。

编译处理要求：

1. 每次复制一批 FPU 代码后，立即尝试编译。
    
2. 如果出现编译错误，优先从 Bochs 源码中继续补齐缺失的类型、宏、辅助函数或字段。
    
3. 不要用空实现绕过关键 FPU 行为。
    
4. 不要为了编译通过而删除关键语义。
    
5. 如果某个依赖明显属于完整 Bochs 框架，但当前工程不需要动态能力，可以将其静态化或写最小胶水适配。
    
6. 最终必须保证当前工程可以成功编译运行。
    

每次修改后，请说明：

1. 本次补充了哪些 FPU / 浮点指令类别；
    
2. 对应复制了 Bochs 哪些源码文件或函数；
    
3. 哪些代码做了裁剪；
    
4. 哪些动态依赖被静态化；
    
5. 当前还剩哪些 FPU 指令没有补；
    
6. 编译是否通过；
    
7. 如果编译失败，失败原因是什么，下一步应补哪些 Bochs 依赖；
    
8. 如果编译通过，说明当前运行验证结果。
    

本轮完成标准：

1. 当前工程中主要 FPU / 浮点相关指令已经补齐；
    
2. 相关代码主要来自 Bochs 源码，而不是自己重写；
    
3. 工程可以成功编译；
    
4. 工程可以基本运行；
    
5. 不再因为明显缺失 FPU / 浮点指令而直接中断；
    
6. 本轮任务到此结束，不需要继续推进 MD5/RIP 对齐。
    

这些指令中可能也有没初始化的变量，你要将其初始化一下比如下面遇到的这个错误：

Run-Time Check Failure #3 - The variable 'ss_descriptor' is being used without being initialized.