你现在继续接手当前文件夹下的对标 Bochs 最小热路径提取工程。

当前工程的基本情况如下：

本轮目标：

0. 当前任务的最高优先级是尽快推进到 8000 万条指令对齐。所有 debug、代码补充、设备裁剪、源码复制和打印策略调整，都应服务于这个目标，不要在非关键问题上消耗过多时间。

1. 从当前 550 万附近的分叉继续向前定位第一条稳定分叉指令。
2. 判断分叉是否由设备初始化、设备状态、IO handler、中断或时钟引起。
3. 如果确定是我们工程缺少 Bochs 逻辑，则从 Bochs 源码中复制最小必要代码到我们工程中。
4. 修复后继续推进 MD5/RIP 对齐。
5. 本轮尽量把对齐指令数继续向后推进，不只停留在 550 万附近。

1. 当前文件夹中运行的是我们自己从 Bochs 源码中裁剪出来的最小热路径工程。
2. Bochs 源码侧和我们自己的工程侧都已经植入了 MD5 计算逻辑。
3. 两边目前都采用单步执行。
4. 每条指令执行后，通过比对 MD5 和 RIP 判断当前执行状态是否一致。
5. MD5 用于比对寄存器状态，RIP 用于比对控制流。
6. 当前工程的核心原则不变：不是重写 Bochs，不是自己实现模拟器，而是从 Bochs 源码中复制、裁剪、粘贴最小必要代码到我们自己的工程中。

目前已知分叉点如下：

Bochs 源码侧输出：

    5500000: f84e84c5 RIP=6684

我们自己的工程输出：

    5500000: bf3c01e2 RIP=6705

目前可以基本确定，这次分叉不是时钟中断导致的。原因是：Bochs 源码侧已经重复运行过两遍，输出结果一致，说明源码侧这段路径是稳定的。因此，这里大概率已经出现了真实分叉点。

根据当前经验判断，本次分叉高度怀疑和设备相关，尤其可能和以下内容有关：

1. 设备初始化顺序；
2. 设备插件加载顺序；
3. IO handler 注册顺序；
4. PCI / ISA / CMOS / DMA / PIC / PIT / harddrv / pci_ide 等设备初始化副作用；
5. 某些设备状态没有初始化；
6. 某些设备路径虽然加载了，但关键静态值或寄存器状态没有和 Bochs 对齐；
7. 某些源码中的动态配置值被我们静态化时写错；
8. 某些设备相关的副作用在裁剪时被误删。

当前可参考的设备初始化代码是 `bx_devices_c::init(BX_MEM_C* newmem)`。这段代码中已经手动补充了一批设备插件加载逻辑，例如：

- unmapped
- biosdev
- speaker
- extfpuirq
- parallel
- serial
- gameport
- iodebug
- ne2k
- usb_uhci
- pci
- pci2isa
- acpi
- hpet
- cmos
- dma
- pic
- pit
- vga
- floppy
- ioapic
- keyboard
- harddrv
- pci_ide

请你重点对照 Bochs 源码中的设备初始化顺序、插件加载顺序、handler 注册顺序、设备状态初始化顺序，检查我们当前代码是否缺失了某些关键副作用。

你可以参考我提供的 `bx_devices_c::init()` 代码进行修改，但不要凭空重写逻辑。当前原则仍然是：

1. 我们自己的代码 99% 应该来自 Bochs 源码。
2. 几乎不需要自己写新逻辑。
3. 如果 Bochs 源码里已经有对应实现，就优先复制 Bochs 源码。
4. 只做最小裁剪和最小适配。
5. 不要主动设计新的设备模型。
6. 不要主动重构 Bochs 原有逻辑。
7. 不要为了代码看起来更简洁而改变 Bochs 的行为。
8. 不要搬完整 Bochs，只搬当前热路径继续推进所需的最小代码。

对 Bochs 源码侧的要求：

1. Bochs 源码侧是标准答案。
2. 尽量不要修改 Bochs 源码的执行逻辑。
3. 如果需要调试，可以在 Bochs 源码中临时加入打印，用于获取寄存器值、IO 值、设备状态、中断状态、handler 路径等信息。
4. 临时调试打印不能改变 Bochs 的执行行为。
5. 真正需要修复和补充的地方应该是我们自己的工程代码。

对我们自己的工程侧的要求：

1. 找到和 Bochs 源码不一致的第一个稳定分叉点。
2. 在我们自己的代码中 debug。
3. 对照 Bochs 源码确认缺失逻辑。
4. 从 Bochs 源码中复制最小必要代码。
5. 修改我们自己的代码，使其重新和 Bochs 源码侧的 MD5/RIP 对齐。
6. 尽量不要自己手写替代逻辑。
7. 如果必须写适配代码，也只能写胶水层、静态值替换层、接口适配层，不要重写核心行为。

当前已经对齐到了 500 多万条指令。大约 545 万条附近可能仍然能对上，550 万条出现明确分叉。因此本轮应围绕 545 万到 550 万之间继续缩小范围，定位第一条真正分叉的指令。

建议调试策略如下：

1. 先确认 545 万附近是否仍然完全一致。
2. 逐步缩小 545 万到 550 万之间的分叉区间。
3. 可以把打印频率从“每几万条打印几条”临时改为更密集的策略。
4. 定位到可疑区间后，连续打印每一条指令的：
   - step
   - MD5
   - RIP
   - opcode
   - 关键通用寄存器
   - flags
   - 最近 IO 端口访问
   - 最近设备 handler
   - 最近中断状态
5. 如果 RIP 一致但 MD5 不一致，优先怀疑指令执行、寄存器更新、flags 更新、内存访问或 IO 副作用不一致。
6. 如果 RIP 不一致，优先怀疑中断、设备事件、IO 返回值、设备初始化状态或控制流相关副作用不一致。
7. 当前 550 万处分叉时，RIP 已经不一致，因此应重点检查设备路径、中断路径、IO 返回值和初始化顺序。

仍然需要记住时钟中断的影响：

1. 后续设备越来越多，时钟中断、计数器、异步设备状态导致 MD5/RIP 不一致的概率会越来越高。
2. 如果遇到新的 MD5/RIP 不一致，不能立刻判断为指令错误。
3. 应先重新运行一遍 Bochs 源码侧，观察 Bochs 源码侧本次结果是否和上一次一致。
4. 如果 Bochs 源码侧两次运行结果不一致，说明差异大概率来自时钟中断或动态事件。
5. 这种情况下，需要多次运行 Bochs 源码侧，尝试找到一条和我们工程输出一致或最接近的路径。
6. 如果 Bochs 源码侧多次运行结果一致，而我们工程不一致，则说明大概率是我们代码中缺少某段 Bochs 逻辑，或者设备状态没有对齐。

本轮重点怀疑方向：

1. `bx_devices_c::init()` 中设备加载顺序是否和 Bochs 源码真实执行顺序一致。
2. `PLUG_load_plugin()` 的调用顺序是否有遗漏或提前/滞后。
3. `bx_init_plugins()` 的执行时机是否正确。
4. PCI 初始化是否缺少关键字段或 handler。
5. CMOS 寄存器初始化是否完全一致。
6. DMA / PIC / PIT / ioapic 初始化是否存在缺失。
7. harddrv / pci_ide 初始化是否和 Bochs 源码路径一致。
8. 端口 `0x92`、`0x0CF8`、`0x0CFC-0x0CFF` 等 IO handler 注册是否一致。
9. `read_port_to_handler` 和 `write_port_to_handler` 是否完整初始化。
10. 设备插件虽然加载了，但内部 init / reset / register_state / register_timer 等副作用是否被遗漏。
11. 某些 Bochs 中由 SIM 动态配置读取的值是否被我们错误静态化。

另外注意一个工程问题：

后续我们自己的工程可能会生成 `.lock` 文件。这里需要修改我们自己的代码，避免生成 `.lock` 文件。当前工程不需要通过 `.lock` 文件维护完整 Bochs 的运行时锁机制，这类逻辑应尽量裁掉或静态化处理，避免影响重复运行和对齐验证。

本轮目标：

1. 从当前 550 万附近的分叉继续向前定位第一条稳定分叉指令。
2. 判断分叉是否由设备初始化、设备状态、IO handler、中断或时钟引起。
3. 如果确定是我们工程缺少 Bochs 逻辑，则从 Bochs 源码中复制最小必要代码到我们工程中。
4. 修复后继续推进 MD5/RIP 对齐。
5. 本轮尽量把对齐指令数继续向后推进，不只停留在 550 万附近。

每次修改后，请说明：

1. 当前定位到的第一条分叉 step；
2. 分叉时 Bochs 源码侧的 MD5 和 RIP；
3. 分叉时我们工程侧的 MD5 和 RIP；
4. 当时最近的设备路径、IO 路径或中断路径；
5. Bochs 源码侧走到了哪个函数；
6. 我们工程缺少哪段逻辑；
7. 从 Bochs 哪些文件复制了哪些代码；
8. 哪些代码被裁剪；
9. 哪些动态值被静态化；
10. 是否修改了源码侧，如果修改，只能是调试打印；
11. 修改后重新验证到了多少条指令；
12. 是否仍然满足“99% 复制 Bochs 源码，最小裁剪、最小适配”的原则。

最终判断标准不是代码是否完整，也不是代码是否好看，而是：

在尽量少搬 Bochs 代码的前提下，让我们自己的工程继续和 Bochs 源码侧保持 MD5/RIP 对齐，并尽快越过当前 550 万附近的设备分叉点。bochs源码所在的位置是"D:\bochs-3.0"